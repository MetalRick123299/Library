import { render } from '@testing-library/react';
import App from './App';

describe('Main App', () => {
  it('Renders Properly', () => {
    // ARRANGE
    render(<App />);
    // ACT
    // EXPECT
  });
});
